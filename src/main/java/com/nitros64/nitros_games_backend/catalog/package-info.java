/**
 * Reference-data module.
 *
 * <p>The module owns its domain objects, use cases, persistence adapters and
 * HTTP adapters. Other modules may reference catalog domain types, but must not
 * depend on its application, persistence or web packages.</p>
 */
package com.nitros64.nitros_games_backend.catalog;
