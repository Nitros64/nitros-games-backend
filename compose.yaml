name: nitros-games

services:
  keycloak:
    profiles:
      - identity
    image: quay.io/keycloak/keycloak:26.7.0
    command:
      - start-dev
      - --import-realm
    restart: unless-stopped
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: ${KEYCLOAK_ADMIN_USERNAME:-admin}
      KC_BOOTSTRAP_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD:?Set KEYCLOAK_ADMIN_PASSWORD}
      KC_HOSTNAME: http://localhost:${KEYCLOAK_PORT:-8081}
      KC_HEALTH_ENABLED: "true"
      NITROS_GAMES_CLI_SECRET: ${NITROS_GAMES_CLI_SECRET:?Set NITROS_GAMES_CLI_SECRET}
    ports:
      - ${KEYCLOAK_PORT:-8081}:8080
    volumes:
      - ./docker/keycloak:/opt/keycloak/data/import:ro
    security_opt:
      - no-new-privileges:true
    mem_limit: ${KEYCLOAK_MEMORY_LIMIT:-768m}
    pids_limit: 300
    logging:
      options:
        max-size: 10m
        max-file: "3"

  mysql:
    image: mysql:8.4.11
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: nitrosgames
      MYSQL_USER: ${DB_USERNAME:-nitros}
      MYSQL_PASSWORD: ${DB_PASSWORD:?Set DB_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:?Set DB_ROOT_PASSWORD}
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test:
        - CMD-SHELL
        - MYSQL_PWD=$${MYSQL_PASSWORD} mysqladmin ping -h 127.0.0.1 -u$${MYSQL_USER} --silent
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    security_opt:
      - no-new-privileges:true
    mem_limit: ${MYSQL_MEMORY_LIMIT:-1g}
    pids_limit: 300
    logging:
      options:
        max-size: 10m
        max-file: "3"

  api:
    image: nitros-games-backend:local
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    init: true
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      SPRING_PROFILES_ACTIVE: prod
      DB_URL: jdbc:mysql://mysql:3306/nitrosgames?sslMode=DISABLED&allowPublicKeyRetrieval=true
      DB_USERNAME: ${DB_USERNAME:-nitros}
      DB_PASSWORD: ${DB_PASSWORD:?Set DB_PASSWORD}
      APP_STORAGE_HOST_IMAGES_DIRECTORY: /var/lib/nitros-games/host-images
      APP_STORAGE_MAX_FILE_SIZE: ${APP_STORAGE_MAX_FILE_SIZE:-10MB}
      APP_STORAGE_MAX_REQUEST_SIZE: ${APP_STORAGE_MAX_REQUEST_SIZE:-11MB}
      APP_SECURITY_ALLOWED_ORIGINS: ${APP_SECURITY_ALLOWED_ORIGINS:-http://localhost:4200}
      OAUTH2_ISSUER_URI: ${OAUTH2_ISSUER_URI:-http://localhost:8081/realms/nitros-games}
      OAUTH2_JWK_SET_URI: ${OAUTH2_JWK_SET_URI:-http://keycloak:8080/realms/nitros-games/protocol/openid-connect/certs}
      OAUTH2_AUDIENCE: ${OAUTH2_AUDIENCE:-nitros-games-api}
      APP_LOG_LEVEL: ${APP_LOG_LEVEL:-INFO}
      DB_POOL_MIN_IDLE: ${DB_POOL_MIN_IDLE:-2}
      DB_POOL_MAX_SIZE: ${DB_POOL_MAX_SIZE:-10}
    ports:
      - ${APP_PORT:-8080}:8080
    volumes:
      - host-images:/var/lib/nitros-games/host-images
    read_only: true
    tmpfs:
      - /tmp:size=64m,mode=1777
    security_opt:
      - no-new-privileges:true
    mem_limit: ${APP_MEMORY_LIMIT:-768m}
    pids_limit: 200
    stop_grace_period: 30s
    healthcheck:
      test:
        - CMD
        - wget
        - -q
        - -O
        - /dev/null
        - http://127.0.0.1:8080/actuator/health/readiness
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s
    logging:
      options:
        max-size: 10m
        max-file: "3"

volumes:
  mysql-data:
  host-images:
